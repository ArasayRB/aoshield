import { Form } from "./form";

export const Main = () => {
    //let url = API_ENDPOINT+'public/v2/users';
    //const {data,error,loading} = useFetch(url);
    return (
        <div>     
            <Form key='form-user-create'/>            
        </div>
    );
}